#include <iostream>
using namespace std;
int main()
{
  // operators in c++
  // arithmetic operators
  int a = 5, b = 3;
  // cout << a + b << endl;
  // cout << a - b << endl;
  // cout << a * b << endl;
  // cout << a / b << endl;
  // cout << a % b << endl;
  // increment and decrement operators
  // cout << a++ << endl;
  // cout << a << endl;
  // cout << ++a << endl;
  // cout << a << endl;
  // cout << a-- << endl;
  // cout << a << endl;
  // cout << --a << endl;
  // cout << a << endl;
  // assignment operators
  // comprassion operators
  // int x = (a > b);
  // cout << x << endl;
  // x = (a < b);
  // cout << x << endl;
  // x = (a >= b);
  // cout << x << endl;
  // x = (a <= b);
  // cout << x << endl;
  // x = (a == b);
  // cout << x << endl;
  // x = (a != b);
  // cout << x << endl;
  // logical operators
  // int x = (a == 5) && (b == 3);
  // cout << x << endl;
  // x = (a == 5) && (b == 4);
  // cout << x << endl;
  // x = (a == 5) || (b == 4);
  // cout << x << endl;
  // x = (a == 4) || (b == 4);
  // cout << x << endl;
  // x = !(a == 4);
  // cout << x << endl;
  // x = !(a == 5);
  // cout << x << endl;
  // bitwise operators
  int y = ((a & b) >> 2);
  cout << y << endl;
  y = ((a | b) >> 2);
  cout << y << endl;
  y = ((a ^ b) >> 2);
  cout << y << endl;
  y = ((a ^ b) << 2);
  cout << y << endl;
}